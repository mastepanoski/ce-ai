# Exploration
Enforcement options evaluated in-issue: hard-fail CI job vs documentation-
first with template forecast. Documentation-first chosen for this pass; the
CI numstat job is a fast-follow once the forecast data exists on real PRs.
Counting contract pre-empts risk R3 (lockfiles/generated/binary inflation);
orthogonality principle pre-empts R5; docs-only exemption pre-empts R6.
