# Milestone Archive Rollup: 2026-Q3

- **Compaction Date:** 2026-09-13
- **Total Changes Compacted:** 110
- **Raw Archive Tarball:** [archive-2026-Q3.tar.gz](archive-2026-Q3.tar.gz)

## Compacted Changes Summary

| Feature | Archive Date | Tasks Progress | Proposal Summary |
| :--- | :--- | :--- | :--- |
| [adoption-block-ssot-v2](#adoption-block-ssot-v2) | 2026-08-24 | 0/17 | Agents working in projects adopted via `ce-ai init-prj` receive a managed `AG... |
| [adoption-block-staleness-alignment](#adoption-block-staleness-alignment) | 2026-08-24 | 0/5 | `ce-ai status` outputs generic `DRIFT DETECTED` for adopted projects even whe... |
| [adoption-test-hardening-and-stale-drift](#adoption-test-hardening-and-stale-drift) | 2026-08-24 | 0/6 | Post-release review of adoption block v2 surfaced coverage gaps and an operat... |
| [agy-adapter-audit-refinements](#agy-adapter-audit-refinements) | 2026-08-24 | 9/9 | Audit findings across the native harness adapters identified specific documen... |
| [agy-native-harness-adapter](#agy-native-harness-adapter) | 2026-08-24 | 14/14 | Previously, `ce-ai` treated Google Antigravity (`agy`) as a generic JSON harn... |
| [automated_release_and_sync_verification](#automated_release_and_sync_verification) | 2026-08-24 | 6/6 | Provide deterministic, automated release packaging on `main` merges and itemi... |
| [backup_restore_management](#backup_restore_management) | 2026-08-24 | 15/15 | - **Current Limitation**: When configuration issues or unwanted skill changes... |
| [ce-ai](#ce-ai) | 2026-08-24 | 34/34 | Provide a single Rust CLI, `ce-ai`, that installs, syncs, upgrades, and confi... |
| [claude-code-native-harness-adapter](#claude-code-native-harness-adapter) | 2026-08-24 | 15/15 | The current `claude` harness adapter incorrectly points to `~/.config/claude/... |
| [codex-adapter-audit-refinements](#codex-adapter-audit-refinements) | 2026-08-24 | 12/12 | 1. `ce-ai` checked `CODEX_CONFIG_DIR` instead of the official `CODEX_HOME` en... |
| [codex-native-harness-adapter](#codex-native-harness-adapter) | 2026-08-24 | 18/18 | `ce-ai` currently lacks native support for the Codex AI harness. Non-OpenCode... |
| [companion-tool-readiness-and-freshness](#companion-tool-readiness-and-freshness) | 2026-08-24 | 0/19 | Currently, `ce-ai` detects whether companion sidecar binaries (Engram, CodeGr... |
| [context_exhaustion_resilience](#context_exhaustion_resilience) | 2026-08-24 | 15/15 | In long AI agent sessions, context window compaction and token dilution degra... |
| [copilot-adapter-audit-refinements](#copilot-adapter-audit-refinements) | 2026-08-24 | 12/12 | - **Goal**: Refine the native GitHub Copilot CLI adapter based on audit feedb... |
| [copilot-native-harness-adapter](#copilot-native-harness-adapter) | 2026-08-24 | 18/18 | `ce-ai` currently lacks native support for the GitHub Copilot CLI / extension... |
| [cursor-native-harness-adapter](#cursor-native-harness-adapter) | 2026-08-24 | 16/16 | - **Change Name**: `cursor-native-harness-adapter` - **Issue Reference**: #17... |
| [custom-harness-r4](#custom-harness-r4) | 2026-08-24 | 12/12 | Audit of the multi-harness series (target 11/11) found Requirement 4 of `mult... |
| [dry-run-purity](#dry-run-purity) | 2026-08-24 | 5/5 | The global `--dry-run` flag is intended as a 100% side-effect-free preview. H... |
| [error-propagation-transactional-cleanup](#error-propagation-transactional-cleanup) | 2026-08-24 | 0/5 | Commands `uninstall`, `deinit-prj`, and `init-prj` contained several `let _ =... |
| [fx-adapter-audit-refinements](#fx-adapter-audit-refinements) | 2026-08-24 | 11/11 | Audit findings across the native harness adapters identified specific documen... |
| [fx-native-harness-adapter](#fx-native-harness-adapter) | 2026-08-24 | 13/13 | `ce-ai` previously mapped `fx` to a fictional `~/.config/fx/fx.json` file wit... |
| [grok-adapter-audit-refinements](#grok-adapter-audit-refinements) | 2026-08-24 | 10/10 | - **Goal**: Address audit findings for the xAI Grok Build CLI native harness ... |
| [grok-native-harness-adapter](#grok-native-harness-adapter) | 2026-08-24 | 19/19 | Previously, `ce-ai` treated Grok as a generic JSON harness writing OpenCode s... |
| [harness-containment-safety-gate](#harness-containment-safety-gate) | 2026-08-24 | 6/6 | `src/commands/install.rs` previously hardcoded `target_base_dir` (`~/.config/... |
| [homebrew-tap-automation](#homebrew-tap-automation) | 2026-08-24 | 7/8 | `ce-ai` is distributed via the Homebrew tap `mastepanoski/ce-ai` (repository ... |
| [installer-download-resilience](#installer-download-resilience) | 2026-08-24 | 0/3 | Both installers (`scripts/install.ps1`, `scripts/install.sh`) resolve `releas... |
| [kimi-adapter-audit-refinements](#kimi-adapter-audit-refinements) | 2026-08-24 | 10/10 | Audit findings on Issue #178 (Kimi Code CLI Native Harness Adapter) identifie... |
| [kimi-native-harness-adapter](#kimi-native-harness-adapter) | 2026-08-24 | 14/14 | Currently, `ce-ai` treats Kimi as a generic JSON harness at `~/.kimi/config.j... |
| [model-defaults-tui-orchestrator](#model-defaults-tui-orchestrator) | 2026-08-24 | 19/19 | 1. ce-ai writes `agent.<slot>.model` assignments into `opencode.json` but not... |
| [model_assignments_resilience_and_tui](#model_assignments_resilience_and_tui) | 2026-08-24 | 0/5 | Currently, `ce-ai` writes `agent.<slot>` entries into `~/.config/opencode/ope... |
| [multi-harness-reconciliation](#multi-harness-reconciliation) | 2026-08-24 | 13/13 | Following the implementation and release of all 10 real native adapters (`Ope... |
| [multi_harness_operations](#multi_harness_operations) | 2026-08-24 | 10/10 | Users running `ce-ai` across multiple AI agent harnesses (`opencode`, `claude... |
| [multi_harness_support](#multi_harness_support) | 2026-08-24 | 12/12 | `ce-ai` v0.2.0 manages Compound Engineering plugin installation and model rol... |
| [pi-native-harness-adapter](#pi-native-harness-adapter) | 2026-08-24 | 12/12 | `ce-ai`'s previous stub handling for `pi` assumed a fictional `~/.pi/config.j... |
| [project_adoption_init_prj](#project_adoption_init_prj) | 2026-08-24 | 0/12 | Today `ce-ai install` only mutates global surfaces (`opencode.json`, managed ... |
| [skill_registry_engine](#skill_registry_engine) | 2026-08-24 | 0/5 | `ce-ai` currently distributes skills and loader scripts across 12 supported A... |
| [sync-native-registration-fix](#sync-native-registration-fix) | 2026-08-24 | 6/6 | The multi-harness audit series surfaced follow-up debt while implementing `cu... |
| [sync-registration-strategy](#sync-registration-strategy) | 2026-08-24 | 4/4 | PR #206 fixed the sync fallthrough by giving each harness its own arm, but th... |
| [sync-watch-loop](#sync-watch-loop) | 2026-08-24 | 4/4 | Issue #159 notes that `ce-ai sync --watch` currently prints a monitoring mess... |
| [token-efficiency-and-context-quality-audit](#token-efficiency-and-context-quality-audit) | 2026-08-24 | 0/6 | While `ce-ai doctor` validates environment invariants and fails on errors, `c... |
| [tools-install-real-sidecar-registration](#tools-install-real-sidecar-registration) | 2026-08-24 | 0/5 | `ce-ai tools install <tool>` currently outputs fake success lines without mod... |
| [tui_workflow_stage_exec](#tui_workflow_stage_exec) | 2026-08-24 | 13/13 | The Workflow (FSM) panel in the `ce-ai` TUI dashboard (issue #76) advertises ... |
| [upgrade-provenance-sync-verification](#upgrade-provenance-sync-verification) | 2026-08-24 | 0/17 | Three related trust failures in the upgrade/sync pipeline: |
| [v0_5_0_roadmap](#v0_5_0_roadmap) | 2026-08-24 | 9/9 | Completes the v0.5.0 release milestone by addressing Issues #7, #9, and #10: ... |
| [v0_6_0_roadmap](#v0_6_0_roadmap) | 2026-08-24 | 9/9 | While `ce-ai` v0.5.0 introduced workspace scope isolation (`--scope workspace... |
| [v0_7_0_roadmap](#v0_7_0_roadmap) | 2026-08-24 | 11/11 | While `ce-ai` v0.6.0 established proactive workflow observability, live TUI g... |
| [v0_8_0_roadmap](#v0_8_0_roadmap) | 2026-08-24 | 12/12 | While `ce-ai` v0.7.0 delivered repository-level workspace overrides and compl... |
| [v0_9_0_roadmap](#v0_9_0_roadmap) | 2026-08-24 | 10/10 | Following the successful completion of Release v0.8.0, `ce-ai` requires a ded... |
| [v1_0_0_roadmap](#v1_0_0_roadmap) | 2026-08-24 | 0/9 | `ce-ai` has reached production maturity across 12 AI coding agent harnesses w... |
| [version_update_checker](#version_update_checker) | 2026-08-24 | 0/9 | Closes #14. Harnesses with `source: local` display `version: local`, making i... |
| [workflow-fsm-engine](#workflow-fsm-engine) | 2026-08-24 | 7/7 | Issue #156 notes that `ce-ai workflow` currently abuses `State.last_update_ch... |
| [worktree_safety_protection](#worktree_safety_protection) | 2026-08-24 | 1/6 | When pair programming with AI agents, worktrees are created outside the prima... |
| [canonical-skills-adoption](#canonical-skills-adoption) | 2026-08-26 | 12/12 | The compound-engineering release ships its 33 skills at top-level `skills/`, ... |
| [close-determinism-leaks](#close-determinism-leaks) | 2026-08-26 | 15/15 | The determinism audit (2026-08-24) identified two leaks inside the asset/stat... |
| [exit-codes-and-bench-wiring](#exit-codes-and-bench-wiring) | 2026-08-26 | 5/5 | Issues #163 (P1) and #84. - #163: invariant #7 promises exit codes `3` State ... |
| [gate-hardening](#gate-hardening) | 2026-08-26 | 4/4 | Issue #165 (P1): verification gates must verify. 1. `make e2e` reported PASS ... |
| [ideation-retention-ssot-v3](#ideation-retention-ssot-v3) | 2026-08-26 | 11/11 | The SSOT rule calls ideation artifacts (`docs/brainstorms/*.md`, `docs/ideati... |
| [internal-hygiene-registration-spec](#internal-hygiene-registration-spec) | 2026-08-26 | 5/5 | Three debts from the v1.20.x sweep: install.rs still carried the nine-arm cop... |
| [openspec-hygiene-and-error-transparency](#openspec-hygiene-and-error-transparency) | 2026-08-26 | 6/6 | Two debts from the v1.20.0 audit sweep: 1. **The OpenSpec ledger lies by omis... |
| [pr-size-boundaries](#pr-size-boundaries) | 2026-08-26 | 3/3 | Issue #108: adopt Gentle AI-style LOC change boundaries as documented, audita... |
| [sync-verification-matrix-ux](#sync-verification-matrix-ux) | 2026-08-26 | 6/6 | `ce-ai sync` prints a Sync Verification Matrix whose wording misleads users ... |
| [transactional-ops](#transactional-ops) | 2026-08-26 | 7/7 | Issue #166 (P2): `write_atomic` protects individual files, but `install` and ... |
| [tui-e2e-zen](#tui-e2e-zen) | 2026-08-26 | 4/4 | El TUI pasa 8/8 tests pero `every_tui_spawned_vector_satisfies_its_cli_contra... |
| [tui-upgrade-contract-fix](#tui-upgrade-contract-fix) | 2026-08-26 | 3/3 | The TUI "Upgrade Release" panel still spawned `ce-ai upgrade --harness all --... |
| [usage-capture-report](#usage-capture-report) | 2026-08-26 | 7/7 | Issue #78: ce-ai orchestra harnesses pero no hay visibilidad de tokens consum... |
| [workload-guard-citation](#workload-guard-citation) | 2026-08-26 | 1/1 | Precision follow-up to `pr-size-boundaries` after verifying upstream: the 400... |
| [codegraph-init-and-cleanup](#codegraph-init-and-cleanup) | 2026-09-07 | 0/5 | In `ce-ai`, users and companion tools rely on CodeGraph (`.codegraph/` index)... |
| [codegraph-init-happy-path-tests](#codegraph-init-happy-path-tests) | 2026-09-07 | 0/3 | In `ce-ai v1.39.0`, native CodeGraph index initialization support was introdu... |
| [codex-session-start-drift-delivery](#codex-session-start-drift-delivery) | 2026-09-07 | 0/19 | In `ce-ai v1.31.0`–`v1.33.0`, native Turn-0 synchronization was implemented... |
| [cursor-session-start-drift-delivery](#cursor-session-start-drift-delivery) | 2026-09-07 | 0/7 | When developing in Cursor (both desktop IDE and Cursor CLI v0.45+), agent ses... |
| [pedagogical-guardrail-mode](#pedagogical-guardrail-mode) | 2026-09-07 | 0/11 | Junior developers using AI coding agents increasingly fall into **vibe coding... |
| [pi-session-start-drift-delivery](#pi-session-start-drift-delivery) | 2026-09-07 | 0/19 | `ce-ai` provides 0-step Turn-0 `RepoState` synchronization for Claude Code, O... |
| [session-start-drift-delivery](#session-start-drift-delivery) | 2026-09-07 | 0/15 | In `ce-ai v1.30.0`, `workflow resume` implemented live sub-15ms `RepoState` d... |
| [antigravity-pre-invocation-drift-delivery](#antigravity-pre-invocation-drift-delivery) | 2026-09-11 | 7/7 | Google Antigravity CLI does not possess a literal "session start" lifecycle h... |
| [auto-configure-rtk-hook-injection](#auto-configure-rtk-hook-injection) | 2026-09-11 | 26/26 | `rtk` (CLI output token reduction engine) currently has **no auto-registratio... |
| [ce-code-review-mid-tier-slot](#ce-code-review-mid-tier-slot) | 2026-09-11 | 18/18 | In `compound-engineering-v3.24.0`, the `ce-code-review` skill introduced pers... |
| [close-engram-codegraph-registration-parity-gap](#close-engram-codegraph-registration-parity-gap) | 2026-09-11 | 23/23 | Companion MCP servers (`codegraph` and `engram`) are auto-registered during `... |
| [copilot-session-start-drift-delivery](#copilot-session-start-drift-delivery) | 2026-09-11 | 22/22 | In `ce-ai v1.31.0` and `v1.32.0`, native Turn-0 synchronization was delivered... |
| [doctor-git-hooks-convention](#doctor-git-hooks-convention) | 2026-09-11 | 13/13 | The `ce-ai doctor` command includes a git-hooks verification probe (`src/comm... |
| [doctor-mcp-tools-detection](#doctor-mcp-tools-detection) | 2026-09-11 | 16/16 | `ce-ai doctor` and `ce-ai tools status` report companion tools like `context7... |
| [doctor-native-marketplace-divergence](#doctor-native-marketplace-divergence) | 2026-09-11 | 20/20 | `ce-ai` manages its own isolated content tree for `compound-engineering` acro... |
| [doctor-workspace-scope-manifest](#doctor-workspace-scope-manifest) | 2026-09-11 | 13/13 | When `ce-ai install --harness opencode --scope workspace` is executed (as rec... |
| [engram-context-echo-behavioral-risk](#engram-context-echo-behavioral-risk) | 2026-09-11 | 14/14 | During the investigation of Issue #313, a recurring symptom was analyzed wher... |
| [extract-inline-unit-tests](#extract-inline-unit-tests) | 2026-09-11 | 51/51 | Currently, unit tests in `ce-ai` are embedded inline inside the same source f... |
| [fix-init-prj-dry-run-output](#fix-init-prj-dry-run-output) | 2026-09-11 | 6/6 | `ce-ai --dry-run init-prj` is correctly non-destructive, but it reports the *... |
| [grok-turn-0-drift-delivery-evaluation](#grok-turn-0-drift-delivery-evaluation) | 2026-09-11 | 3/3 | Evaluate whether Grok Build CLI (`grok`) supports native Turn-0 conversationa... |
| [hardening-inferred-stage-monotonicity](#hardening-inferred-stage-monotonicity) | 2026-09-11 | 9/9 | In Issue #296 (PR #305, v1.40.0), Workflow FSM auto-checkpointing was introdu... |
| [hermetic-git-test-fixtures](#hermetic-git-test-fixtures) | 2026-09-11 | 3/3 | In Issue #302, 3 tests in `tests/cli.rs` were observed failing intermittently... |
| [install-directory-config-mcp-guard](#install-directory-config-mcp-guard) | 2026-09-11 | 5/5 | When running `ce-ai install --harness all` (or `ce-ai sync`), `ce-ai` iterate... |
| [journal-append-only-linear-arm](#journal-append-only-linear-arm) | 2026-09-11 | 27/27 | In `src/state/journal.rs`, `Journal::arm` re-serializes the entire operation ... |
| [kimi-turn-0-drift-delivery-evaluation](#kimi-turn-0-drift-delivery-evaluation) | 2026-09-11 | 3/3 | Evaluate whether Kimi Code CLI (`kimi`) supports native project-level Turn-0 ... |
| [observe-only-gate-check](#observe-only-gate-check) | 2026-09-11 | 14/14 | Issue #332 originally proposed a blocking gate (hook + validation receipt) to... |
| [opencode-session-start-drift-delivery](#opencode-session-start-drift-delivery) | 2026-09-11 | 26/26 | In `ce-ai v1.31.0`, Turn-0 `RepoState` synchronization was guaranteed for Cla... |
| [openspec-archive-command](#openspec-archive-command) | 2026-09-11 | 15/15 | In `ce-ai v1.44.2` (Issue #323), `probe_unarchived_completed_changes` was int... |
| [self-update](#self-update) | 2026-09-11 | 15/15 | `ce-ai upgrade` (and the TUI Upgrade Release flow) exclusively upgrades the c... |
| [sequential-thinking-skill](#sequential-thinking-skill) | 2026-09-11 | 7/7 | In `ce-ai`, `sequential-thinking` is currently defined as an on-demand compan... |
| [ship-readiness-observe-only](#ship-readiness-observe-only) | 2026-09-11 | 8/8 | Issue #354: the 7-stage cycle is not enforced at the Ship boundary. An agent ... |
| [skills-resolve-positional-query](#skills-resolve-positional-query) | 2026-09-11 | 3/3 | When running `ce-ai doctor` or `ce-ai tools status`, `ce-ai` recommends insta... |
| [sync-non-opencode-harnesses](#sync-non-opencode-harnesses) | 2026-09-11 | 14/14 | Currently, `ce-ai sync` and `ce-ai upgrade` treat OpenCode's install manifest... |
| [sync-verification-and-token](#sync-verification-and-token) | 2026-09-11 | 17/17 | When running `ce-ai upgrade` to update to the latest version of the Compound ... |
| [unarchived-completed-changes-detection](#unarchived-completed-changes-detection) | 2026-09-11 | 15/15 | In `ce-ai v1.44.0` (Issue #313), a tasks-desync probe was introduced to recon... |
| [workflow-fsm-auto-checkpoint](#workflow-fsm-auto-checkpoint) | 2026-09-11 | 32/32 | The 7-stage Workflow FSM (`ce-ai workflow status` / `checkpoint` / `resume`, ... |
| [workflow-reset-feature-clearing](#workflow-reset-feature-clearing) | 2026-09-11 | 6/6 | When resetting the 7-stage workflow FSM to Stage 1 (Ideation) from an advance... |
| [workflow-tasks-desync](#workflow-tasks-desync) | 2026-09-11 | 7/7 | In `ce-ai`, the workflow Finite State Machine (`infer_stage_from_repo` in [`s... |
| [workspace-scoped-workflow-and-gitignore](#workspace-scoped-workflow-and-gitignore) | 2026-09-11 | 19/19 | 1. **Global Workflow Collision in FSM**: `State.workflow: Option<WorkflowStat... |
| [zero-friction-release-fallback](#zero-friction-release-fallback) | 2026-09-11 | 17/17 | When running `ce-ai install` or `ce-ai upgrade` without an explicit GitHub Pe... |
| [zero-step-drift-recovery](#zero-step-drift-recovery) | 2026-09-11 | 21/21 | Standard AI agent runtimes condition future tool execution on accumulated con... |
| [blocking-gate-check](#blocking-gate-check) | 2026-09-12 | 24/24 | In Spec-Driven Development and Compound Engineering, production code under `s... |
| [archive-compaction](#archive-compaction) | 2026-09-13 | 33/33 | In Compound Engineering, `openspec/changes/archive/` serves as the project's ... |
| [doc-debt-engine-and-probes](#doc-debt-engine-and-probes) | 2026-09-13 | 31/31 | In Compound Engineering, documentation acts as the project's living contract ... |

## Detailed Specifications

### adoption-block-ssot-v2

- **Original Folder:** `adoption-block-ssot-v2`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/17

#### Problem Statement
Agents working in projects adopted via `ce-ai init-prj` receive a managed `AGENTS.md` block that mandates the 7-stage cycle and OpenSpec but gives no rule about how ideation artifacts relate to OpenSpec. Result: agents maintain `docs/brainstorms/` / `docs/ideation/` as paralle...

#### Acceptance Criteria & Specification
WHEN `ce-ai init-prj <path> --tier full` renders the managed block THEN the block MUST contain a "Single Source of Truth Rule" section stating that ideation artifacts are disposable inputs to be distilled into OpenSpec, and that ideation skills are skipped when requirements and approach are clear.

---

### adoption-block-staleness-alignment

- **Original Folder:** `adoption-block-staleness-alignment`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/5

#### Problem Statement
`ce-ai status` outputs generic `DRIFT DETECTED` for adopted projects even when the on-disk block is simply an older template version (`v < BLOCK_VERSION`). In contrast, `ce-ai doctor` provides a targeted upgrade command (`re-run ce-ai init-prj --tier <tier> to upgrade`).

#### Acceptance Criteria & Specification
- **Change:** `adoption-block-staleness-alignment` - **Issue:** #149 ---

---

### adoption-test-hardening-and-stale-drift

- **Original Folder:** `adoption-test-hardening-and-stale-drift`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/6

#### Problem Statement
Post-release review of adoption block v2 surfaced coverage gaps and an operator-experience gap:

#### Acceptance Criteria & Specification
WHEN a project's AGENTS.md contains a hand-written v1 block with LF endings plus surrounding user content THEN re-running init-prj replaces only the marker-delimited region with the v2 block, preserving user content and LF endings.

---

### agy-adapter-audit-refinements

- **Original Folder:** `agy-adapter-audit-refinements`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 9/9

#### Problem Statement
Audit findings across the native harness adapters identified specific documentation and behavior clarifications for the Google Antigravity (`agy`) adapter: 1. `ANTIGRAVITY_CONFIG_DIR` and `GEMINI_HOME` are custom `ce-ai` relocation extensions not present in official Google Ant...

#### Acceptance Criteria & Specification
WHEN inspecting Google Antigravity adapter configuration, THEN `ANTIGRAVITY_CONFIG_DIR` and `GEMINI_HOME` SHALL be documented in OpenSpec design as `ce-ai` extension conventions for custom directory relocation. WHEN adopting or de-adopting a project for Google Antigravity,

---

### agy-native-harness-adapter

- **Original Folder:** `agy-native-harness-adapter`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 14/14

#### Problem Statement
Previously, `ce-ai` treated Google Antigravity (`agy`) as a generic JSON harness pointing to `~/.gemini/antigravity-cli/antigravity.json`. However, official Google Antigravity documentation specifies: - Global MCP configuration lives at `~/.gemini/config/mcp_config.json` (unde...

#### Acceptance Criteria & Specification
WHEN `ce-ai` determines paths for `HarnessKind::Agy` THEN it MUST check environment variable `$ANTIGRAVITY_CONFIG_DIR` first, then `$GEMINI_HOME`, and default to `<home_dir>/.gemini` AND it MUST locate the native MCP config file at `<harness_dir>/config/mcp_config.json`.

---

### automated_release_and_sync_verification

- **Original Folder:** `automated_release_and_sync_verification`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 6/6

#### Problem Statement
Provide deterministic, automated release packaging on `main` merges and itemized file-hash verification for `ce-ai sync`.

#### Acceptance Criteria & Specification
- **WHEN** commits land on `main`, - **THEN** `.github/workflows/release.yml` MUST automatically generate a release tag, build release binaries for Linux/macOS/Windows, and publish a GitHub Release. - **WHEN** `ce-ai sync` runs (or is invoked from TUI),

---

### backup_restore_management

- **Original Folder:** `backup_restore_management`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 15/15

#### Problem Statement
- **Current Limitation**: When configuration issues or unwanted skill changes occur, users must either run `ce-ai uninstall` (which only restores the latest backup) or manually navigate the file system inside `~/.ce-ai/backups/`. - **User Pain Point**: There is no command to v...

#### Acceptance Criteria & Specification
**Feature Name**: `backup_restore_management` **Status**: Specification Approved ---

---

### ce-ai

- **Original Folder:** `ce-ai`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 34/34

#### Problem Statement
Provide a single Rust CLI, `ce-ai`, that installs, syncs, upgrades, and configures the `compound-engineering-plugin` (CE) for AI coding harnesses. Today users must run harness-specific install steps and manually edit config files to assign models to subagents. `ce-ai` automate...

#### Acceptance Criteria & Specification
- GIVEN network access - WHEN `ce-ai install` runs - THEN the latest release is cached and its digest recorded.

---

### claude-code-native-harness-adapter

- **Original Folder:** `claude-code-native-harness-adapter`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 15/15

#### Problem Statement
The current `claude` harness adapter incorrectly points to `~/.config/claude/claude.json` and writes OpenCode-specific JSON keys (`plugin`, `skills.paths`). Claude Code (Anthropic) does not read `~/.config/claude` or OpenCode keys. Its real home directory is `~/.claude` (respe...

#### Acceptance Criteria & Specification
- WHEN `HarnessKind::Claude.harness_dir(&home)` is called THEN it MUST return `CLAUDE_CONFIG_DIR` if set, else `home.join(".claude")`. - WHEN `ClaudeAdapter::default_config_path(&home)` is called THEN: - If `~/.claude/settings.json` exists and contains `"mcpServers"`, it MUST return `~/.claude/settings.json`.

---

### codex-adapter-audit-refinements

- **Original Folder:** `codex-adapter-audit-refinements`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 12/12

#### Problem Statement
1. `ce-ai` checked `CODEX_CONFIG_DIR` instead of the official `CODEX_HOME` environment variable used by Codex CLI to relocate its configuration directory. 2. `spec.md` (R3) in `codex-native-harness-adapter` contained a minor spec drift regarding `AGENTS.md` vs `.codex/AGENTS.m...

#### Acceptance Criteria & Specification
- WHEN `CODEX_HOME` is set in the environment THEN `harness_dir(HarnessKind::Codex)` and `default_config_path` MUST resolve to `$CODEX_HOME` and `$CODEX_HOME/config.toml`. - WHEN `CODEX_HOME` is not set THEN `harness_dir` MUST default to `$HOME/.codex` and `default_config_path` MUST default to `$HOME/.codex/config.t...

---

### codex-native-harness-adapter

- **Original Folder:** `codex-native-harness-adapter`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 18/18

#### Problem Statement
`ce-ai` currently lacks native support for the Codex AI harness. Non-OpenCode harnesses were defaulting to synthetic JSON paths. Codex requires native TOML configuration in `~/.codex/config.toml` using `[mcp_servers.<name>]` tables, skills stored in `~/.codex/skills/`, and pro...

#### Acceptance Criteria & Specification
- WHEN `harness_dir(HarnessKind::Codex)` is called THEN it MUST return `$CODEX_HOME` if set, otherwise `$HOME/.codex`. - WHEN `default_config_path` is called THEN it MUST return `$CODEX_HOME/config.toml` if set, otherwise `$HOME/.codex/config.toml`. - WHEN `is_installed_on_host` is called for `HarnessKind::Codex` TH...

---

### companion-tool-readiness-and-freshness

- **Original Folder:** `companion-tool-readiness-and-freshness`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/19

#### Problem Statement
Currently, `ce-ai` detects whether companion sidecar binaries (Engram, CodeGraph, Context7, RTK) exist on `PATH` or in default paths, but it does NOT perform **version freshness checks**, **skill recommendations** (e.g. `sequential-thinking`), or **self-update suggestions**.

#### Acceptance Criteria & Specification
- **Change:** `companion-tool-readiness-and-freshness` - **Issue:** #112 - **Author:** Antigravity AI

---

### context_exhaustion_resilience

- **Original Folder:** `context_exhaustion_resilience`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 15/15

#### Problem Statement
In long AI agent sessions, context window compaction and token dilution degrade LLM recall of multi-page markdown rules. Expecting AI agents to reliably remember governance constraints (such as "never push directly to main" or "wait for 100% green CI matrix status checks") pur...

#### Acceptance Criteria & Specification
**WHEN** `ce-ai doctor` is executed within a git repository tracked on GitHub, **THEN** it SHALL query GitHub branch protection rules for `main` via `gh api repos/{owner}/{repo}/branches/main/protection`. **WHEN** protection is disabled or missing required status checks,

---

### copilot-adapter-audit-refinements

- **Original Folder:** `copilot-adapter-audit-refinements`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 12/12

#### Problem Statement
- **Goal**: Refine the native GitHub Copilot CLI adapter based on audit feedback. 1. **Clean Env Object Replacement**: In `src/harness/copilot.rs`, update `register_copilot_mcp_server` to cleanly overwrite `server_entry.env = env.clone()`. Combined with `#[serde(skip_serializi...

#### Acceptance Criteria & Specification
- WHEN `register_copilot_mcp_server` is called with an empty `env` map THEN it MUST remove any existing `env` key from `mcpServers.<name>` in `~/.copilot/mcp-config.json`. - WHEN `register_copilot_mcp_server` is called with a non-empty `env` map THEN it MUST replace the `env` object cleanly without retaining stale e...

---

### copilot-native-harness-adapter

- **Original Folder:** `copilot-native-harness-adapter`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 18/18

#### Problem Statement
`ce-ai` currently lacks native support for the GitHub Copilot CLI / extension harness. Non-OpenCode harnesses were defaulting to generic paths. GitHub Copilot requires native JSON configuration in `~/.copilot/mcp-config.json` using `mcpServers` object, skills stored in `~/.cop...

#### Acceptance Criteria & Specification
- WHEN `harness_dir(HarnessKind::Copilot)` is called THEN it MUST return `$COPILOT_CONFIG_DIR` if set, otherwise `$HOME/.copilot`. - WHEN `default_config_path` is called THEN it MUST return `$COPILOT_CONFIG_DIR/mcp-config.json` if set, otherwise `$HOME/.copilot/mcp-config.json`. - WHEN `is_installed_on_host` is call...

---

### cursor-native-harness-adapter

- **Original Folder:** `cursor-native-harness-adapter`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 16/16

#### Problem Statement
- **Change Name**: `cursor-native-harness-adapter` - **Issue Reference**: #173 (Umbrella #155) - **Target Version**: 1.9.0

#### Acceptance Criteria & Specification
WHEN `ce-ai install --harness cursor` or `ce-ai tools install <tool> --harness cursor` is invoked, THEN `ce-ai` SHALL write MCP server configurations into `~/.cursor/mcp.json` under the `mcpServers` root key using the `stdio` format. WHEN `ce-ai` modifies `~/.cursor/mcp.json`,

---

### custom-harness-r4

- **Original Folder:** `custom-harness-r4`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 12/12

#### Problem Statement
Audit of the multi-harness series (target 11/11) found Requirement 4 of `multi_harness_support` unimplemented: `ce-ai install --harness custom` announces and parses the mode but performs no real work.

#### Acceptance Criteria & Specification
The custom harness mode SHALL install Compound Engineering assets into user-configured directories with an explicit, fail-fast configuration contract.

---

### dry-run-purity

- **Original Folder:** `dry-run-purity`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 5/5

#### Problem Statement
The global `--dry-run` flag is intended as a 100% side-effect-free preview. However, two commands currently violate this contract: 1. `ce-ai workflow checkpoint` calls `State::save` without inspecting `ctx.dry_run`, writing `state.json` to disk during preview mode. 2. `ce-ai i...

#### Acceptance Criteria & Specification
WHEN any CLI subcommand is executed with `--dry-run`, THEN `ce-ai` SHALL NOT write, modify, delete, or create any files in the configuration directory (`ctx.config_dir`), home directory (`home_dir`), or project workspace (`workspace_dir`). WHEN `ce-ai install` or `ce-ai upgrade` resolves a remote source under `--dry...

---

### error-propagation-transactional-cleanup

- **Original Folder:** `error-propagation-transactional-cleanup`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/5

#### Problem Statement
Commands `uninstall`, `deinit-prj`, and `init-prj` contained several `let _ =` error-suppression lines during filesystem operations (`remove_file`, `remove_dir_all`, `write_atomic`). If an operation failed, `state.json` was updated to claim success anyway, and a positive statu...

#### Acceptance Criteria & Specification
- **Change:** `error-propagation-transactional-cleanup` - **Issue:** #162 (P1) ---

---

### fx-adapter-audit-refinements

- **Original Folder:** `fx-adapter-audit-refinements`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 11/11

#### Problem Statement
Audit findings across the native harness adapters identified specific documentation and behavior clarifications for the Vercel Labs `fx` adapter: 1. `default_config_path` in `src/harness/fx.rs` includes `home.join("mcp.json").exists()`, making path resolution filesystem-depend...

#### Acceptance Criteria & Specification
WHEN `FxAdapter::default_config_path` is called with a directory path, THEN path resolution SHALL NOT query the filesystem with `.exists()`, relying strictly on basename matching. WHEN `unregister_fx_mcp_server` removes an empty configuration file,

---

### fx-native-harness-adapter

- **Original Folder:** `fx-native-harness-adapter`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 13/13

#### Problem Statement
`ce-ai` previously mapped `fx` to a fictional `~/.config/fx/fx.json` file with OpenCode-style schema. According to official `fx` coding agent documentation (fx.sh / `github.com/vercel-labs/fx`): - Managed assets live under `~/.fx/` (environment variable override `$FX_HOME`). -...

#### Acceptance Criteria & Specification
- `harness_dir(Fx)` MUST evaluate `$FX_HOME` if set and non-empty. - If `$FX_HOME` is unset or empty, `harness_dir(Fx)` MUST return `<home_dir>/.fx`. - `is_installed_on_host(Fx)` MUST return `true` if `<harness_dir>` exists or `<home_dir>/.fx` exists.

---

### grok-adapter-audit-refinements

- **Original Folder:** `grok-adapter-audit-refinements`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 10/10

#### Problem Statement
- **Goal**: Address audit findings for the xAI Grok Build CLI native harness adapter. 1. **Flaky Test Environment Race Fix**: In `src/harness/grok.rs`, synchronize environment variable access (`GROK_HOME`) in unit tests using a static `Mutex` lock to prevent intermittent test ...

#### Acceptance Criteria & Specification
- WHEN running unit tests for `GrokAdapter` in parallel THEN environment variable mutations (`GROK_HOME`) MUST be thread-synchronized via a static `Mutex` guard to prevent test race conditions. - WHEN inspecting `src/harness/generic_json.rs` THEN `HarnessKind::Grok` MUST NOT be mapped to generic JSON files (`.grok/c...

---

### grok-native-harness-adapter

- **Original Folder:** `grok-native-harness-adapter`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 19/19

#### Problem Statement
Previously, `ce-ai` treated Grok as a generic JSON harness writing OpenCode schema to `~/.config/grok/grok.json`. Official xAI documentation confirms Grok Build CLI (`grok`) reads TOML configuration from `~/.grok/config.toml` (`[mcp_servers.<name>]` tables), stores skills in `...

#### Acceptance Criteria & Specification
- WHEN `harness_dir(HarnessKind::Grok)` is called THEN it MUST return `$GROK_HOME` if set, otherwise `$HOME/.grok`. - WHEN `default_config_path` is called THEN it MUST return `$GROK_HOME/config.toml` if set, otherwise `$HOME/.grok/config.toml`. - WHEN `is_installed_on_host` is called for `HarnessKind::Grok` THEN it ...

---

### harness-containment-safety-gate

- **Original Folder:** `harness-containment-safety-gate`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 6/6

#### Problem Statement
`src/commands/install.rs` previously hardcoded `target_base_dir` (`~/.config/opencode/`) for all harnesses during installation and uninstallation.

#### Acceptance Criteria & Specification
- **Change:** `harness-containment-safety-gate` - **Issue:** #157 (P0) ---

---

### homebrew-tap-automation

- **Original Folder:** `homebrew-tap-automation`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 7/8

#### Problem Statement
`ce-ai` is distributed via the Homebrew tap `mastepanoski/ce-ai` (repository `mastepanoski/homebrew-ce-ai`). Today, every release requires a manual, error-prone update of `Formula/ce-ai.rb` in the tap: bumping the version, rewriting four download URLs, and recomputing four SHA...

#### Acceptance Criteria & Specification
- **WHEN** a GitHub release for tag `vX.Y.Z` is published in `mastepanoski/ce-ai` - **THEN** the `bump-homebrew` workflow runs, renders `Formula/ce-ai.rb` with version `X.Y.Z`, four download URLs pointing at the release assets, and their SHA256 digests - **AND** pushes the formula to `mastepanoski/homebrew-ce-ai` br...

---

### installer-download-resilience

- **Original Folder:** `installer-download-resilience`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/3

#### Problem Statement
Both installers (`scripts/install.ps1`, `scripts/install.sh`) resolve `releases/latest` and download the asset **once**, with no retry or fallback. During a release publication window (assets uploading sequentially), the API lookup and/or download return 404 and the installer ...

#### Acceptance Criteria & Specification
WHEN the resolved latest-release asset returns 404 on the first download attempt THEN the installer retries up to 3 total attempts with linear backoff before failing.

---

### kimi-adapter-audit-refinements

- **Original Folder:** `kimi-adapter-audit-refinements`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 10/10

#### Problem Statement
Audit findings on Issue #178 (Kimi Code CLI Native Harness Adapter) identified three refinements: 1. `ce-ai init-prj` writes project rules for Kimi to `.kimi-code/rules/compound-engineering.md`. However, Kimi Code CLI officially loads instructions from `AGENTS.md` (root), `.ki...

#### Acceptance Criteria & Specification
> **Note**: Amends Requirement R3 of `openspec/changes/kimi-native-harness-adapter/spec.md` by updating the target project rule adoption file from `.kimi-code/rules/compound-engineering.md` to `.kimi-code/AGENTS.md`. - WHEN running `init-prj` for a project containing `.kimi-code/` THEN `ce-ai` MUST write the `CE-AI ...

---

### kimi-native-harness-adapter

- **Original Folder:** `kimi-native-harness-adapter`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 14/14

#### Problem Statement
Currently, `ce-ai` treats Kimi as a generic JSON harness at `~/.kimi/config.json`. Official Moonshot AI documentation (Kimi Code CLI, `kimi`) specifies `~/.kimi-code/` as the native configuration root (overridden by `$KIMI_CODE_HOME`), `~/.kimi-code/mcp.json` for MCP server re...

#### Acceptance Criteria & Specification
- WHEN resolving `harness_dir(Kimi)` THEN `ce-ai` MUST check `$KIMI_CODE_HOME` first, falling back to `~/.kimi-code`. - Legacy `~/.kimi/config.json` path is bypassed by native Kimi adapter operations. - WHEN registering MCP servers for Kimi THEN `ce-ai` MUST write entries to `mcp.json` under top-level key `mcpServer...

---

### model-defaults-tui-orchestrator

- **Original Folder:** `model-defaults-tui-orchestrator`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 19/19

#### Problem Statement
1. ce-ai writes `agent.<slot>.model` assignments into `opencode.json` but nothing seeds sane defaults at install time; every assignment depends on manual `ce-ai models set`. 2. There is no orchestrator agent slot named `ce-ai`; slots are per-stage only (`ce-brainstorm`, `ce-pl...

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai install` completes against an `opencode.json` whose agent map lacks models for the default slots - **THEN** each default slot (including `ce-ai`) receives its documented default `provider/model` in both `opencode.json` and `state.json`

---

### model_assignments_resilience_and_tui

- **Original Folder:** `model_assignments_resilience_and_tui`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/5

#### Problem Statement
Currently, `ce-ai` writes `agent.<slot>` entries into `~/.config/opencode/opencode.json` when setting model assignments (e.g. `ce-brainstorm`). However, if `~/.ce-ai/state.json` is missing, corrupted, or reset during reinstall/resync operations, `state.json` loses its `model_a...

#### Acceptance Criteria & Specification
WHEN `ce-ai doctor` is executed AND an `agent.<slot>` assignment exists in `opencode.json` that is missing from `state.json` (or vice-versa), THEN `ce-ai doctor` SHALL report a diagnostic finding warning of model assignment drift and recommending `ce-ai sync`. WHEN `ce-ai sync` is executed, THEN it SHALL reconcile m...

---

### multi-harness-reconciliation

- **Original Folder:** `multi-harness-reconciliation`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 13/13

#### Problem Statement
Following the implementation and release of all 10 real native adapters (`OpenCode`, `Cursor`, `Claude`, `Codex`, `Copilot`, `Grok`, `Kimi`, `Agy`, `Pi`, `Fx`), four cross-cutting issues require reconciliation: 1. **Issue #155 & #183**: `README.md` and OpenSpec `multi_harness_...

#### Acceptance Criteria & Specification
WHEN viewing `README.md` or OpenSpec specifications, THEN `ce-ai` SHALL document the 10 real native adapters (`opencode`, `claude`, `pi`, `cursor`, `copilot`, `codex`, `grok`, `kimi`, `agy`, `fx`) and their native formats, and `openspec/changes/multi_harness_support/spec.md` SHALL be reconciled with the native adapt...

---

### multi_harness_operations

- **Original Folder:** `multi_harness_operations`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 10/10

#### Problem Statement
Users running `ce-ai` across multiple AI agent harnesses (`opencode`, `claude`, `pi`, `cursor`, `copilot`, `kimi`, `agy`, etc.) experience ambiguity during `sync`, `upgrade`, and TUI interactions. Operations should default to updating all installed host harnesses while protect...

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai sync` or `ce-ai upgrade` is executed with `--harness all` (or no harness flag), - **THEN** `ce-ai` MUST iterate through all host-installed harnesses, performing sync/upgrade for each target and outputting itemized progress. - **WHEN** `ce-ai upgrade` targets a harness whose installation source is `...

---

### multi_harness_support

- **Original Folder:** `multi_harness_support`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 12/12

#### Problem Statement
`ce-ai` v0.2.0 manages Compound Engineering plugin installation and model role assignments exclusively for the **OpenCode** harness. However, developers work across a variety of AI coding environments including Claude Code, Pi, Cursor, Copilot, Codex, Grok, Kimi, AGY, DeepSeek...

#### Acceptance Criteria & Specification
**Change Identifier:** `multi_harness_support` --- - **WHEN** the user invokes `ce-ai install --harness <name>`, **THEN** the CLI MUST validate `<name>` against `HarnessKind`. If invalid or de-scoped (such as `deepseek`), return exit code `2` (Usage Error) with actionable error guidance.

---

### pi-native-harness-adapter

- **Original Folder:** `pi-native-harness-adapter`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 12/12

#### Problem Statement
`ce-ai`'s previous stub handling for `pi` assumed a fictional `~/.pi/config.json` file with OpenCode-style schema. According to official `pi` coding agent documentation (pi.dev / `@earendil-works/pi-coding-agent`): - Managed assets live under `~/.pi/agent/` (overridden by `$PI...

#### Acceptance Criteria & Specification
- `harness_dir(Pi)` MUST evaluate `$PI_CODING_AGENT_DIR` if set and non-empty. - If `$PI_CODING_AGENT_DIR` is unset or empty, `harness_dir(Pi)` MUST return `<home_dir>/.pi/agent`. - `is_installed_on_host(Pi)` MUST return `true` if `<harness_dir>` exists or `<home_dir>/.pi` exists.

---

### project_adoption_init_prj

- **Original Folder:** `project_adoption_init_prj`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/12

#### Problem Statement
Today `ce-ai install` only mutates global surfaces (`opencode.json`, managed plugin directory `~/.config/opencode/compound-engineering`). Skills such as `ce-brainstorm` and `ce-plan` are opt-in per session: without explicit governance rules in a target repository's `AGENTS.md`...

#### Acceptance Criteria & Specification
**WHEN** a user or agent executes `ce-ai init-prj [PATH] --tier <full|minimal|orchestrator>` **THEN** `ce-ai` MUST: 1. Resolve the absolute target path (defaulting to current working directory or Git repository root).

---

### skill_registry_engine

- **Original Folder:** `skill_registry_engine`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/5

#### Problem Statement
`ce-ai` currently distributes skills and loader scripts across 12 supported AI coding agent harnesses (`opencode`, `claude`, `pi`, `cursor`, `copilot`, `codex`, `grok`, `kimi`, `agy`, `deepseek`, `fx`, `custom`). File placement and integrity hashes are tracked in `install-mani...

#### Acceptance Criteria & Specification
- **Feature Name**: `skill_registry_engine` - **Issue Reference**: #96 - **Status**: Draft / Proposed

---

### sync-native-registration-fix

- **Original Folder:** `sync-native-registration-fix`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 6/6

#### Problem Statement
The multi-harness audit series surfaced follow-up debt while implementing `custom-harness-r4`: `sync_with()` routes every harness without an explicit arm into the generic OpenCode branch:

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai sync` rebuilds a state entry for `kimi`, `agy`, or `fx`, **THEN** the CLI MUST register the `codegraph` and `engram` MCP servers through that vendor's native config writer and copy the managed skills

---

### sync-registration-strategy

- **Original Folder:** `sync-registration-strategy`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 4/4

#### Problem Statement
PR #206 fixed the sync fallthrough by giving each harness its own arm, but the arm bodies are ~90% copy-paste: eight vendors repeat the identical `register_<vendor>_mcp_server(codegraph)` + `(engram)` + managed-skills-copy

#### Acceptance Criteria & Specification
- **WHEN** a new `HarnessKind` variant is added, **THEN** `registration_spec` MUST fail to compile until the variant is classified in the table or given a dedicated call-site arm.

---

### sync-watch-loop

- **Original Folder:** `sync-watch-loop`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 4/4

#### Problem Statement
Issue #159 notes that `ce-ai sync --watch` currently prints a monitoring message and exits immediately after a single sync pass, failing to detect or repair subsequent drift.

#### Acceptance Criteria & Specification
WHEN `ce-ai sync --watch` is executed, THEN `ce-ai` SHALL continuously monitor managed files at the specified interval until SIGINT or `--max-passes` limit is reached, safely handling single or multiple signal handler registrations in test environments. WHEN `sync --watch` runs a polling tick,

---

### token-efficiency-and-context-quality-audit

- **Original Folder:** `token-efficiency-and-context-quality-audit`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/6

#### Problem Statement
While `ce-ai doctor` validates environment invariants and fails on errors, `ce-ai` lacks an advisory, scored subcommand to measure **token efficiency** and **context quality** across all installed agent harnesses (OpenCode, Claude Code, Cursor, Pi, etc.).

#### Acceptance Criteria & Specification
- **Change:** `token-efficiency-and-context-quality-audit` - **Issue:** #117 ---

---

### tools-install-real-sidecar-registration

- **Original Folder:** `tools-install-real-sidecar-registration`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/5

#### Problem Statement
`ce-ai tools install <tool>` currently outputs fake success lines without modifying config files, initializing tools, or probing post-install health. This creates false readiness reports in `doctor`, `status`, and `audit`.

#### Acceptance Criteria & Specification
- **Change:** `tools-install-real-sidecar-registration` - **Issue:** #158 (P0) ---

---

### tui_workflow_stage_exec

- **Original Folder:** `tui_workflow_stage_exec`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 13/13

#### Problem Statement
The Workflow (FSM) panel in the `ce-ai` TUI dashboard (issue #76) advertises the 7-stage cycle and its mapped commands but is read-only: every real action requires quitting to a terminal. Most advertised commands are agent-harness skills the binary cannot execute, and some sug...

#### Acceptance Criteria & Specification
- **WHEN** the user presses `[Enter]` on the Workflow tab with a readable `state.json` - **THEN** the modal shows the actual status content derived from state (phase/task/checkpoint), not a canned success message

---

### upgrade-provenance-sync-verification

- **Original Folder:** `upgrade-provenance-sync-verification`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/17

#### Problem Statement
Three related trust failures in the upgrade/sync pipeline:

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai upgrade` fetches a release tarball (default path), - **THEN** it MUST persist `{tag, url, archive_sha256, extraction_path}` together with the `managed_asset_digest["tarball"]` entry in one atomic `state.json` write (temp file + rename). - **WHEN** `ce-ai upgrade --to <tag>` runs,

---

### v0_5_0_roadmap

- **Original Folder:** `v0_5_0_roadmap`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 9/9

#### Problem Statement
Completes the v0.5.0 release milestone by addressing Issues #7, #9, and #10: 1. Support `--scope workspace|global` in `ce-ai install`. 2. Manage companion tools (`Engram`, `CodeGraph`, `Context7`, `RTK`) via `ce-ai tools status|install`. 3. Provide Workflow FSM & Progress Reco...

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai install --scope workspace` is executed, - **THEN** `ce-ai` MUST resolve the git repository root and install managed skills and configs locally inside the repository. - **WHEN** `ce-ai tools status` is executed,

---

### v0_6_0_roadmap

- **Original Folder:** `v0_6_0_roadmap`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 9/9

#### Problem Statement
While `ce-ai` v0.5.0 introduced workspace scope isolation (`--scope workspace`), companion tools management (`ce-ai tools`), and progress checkpointing (`ce-ai workflow`), users currently need manual invocation to inspect FSM states and check companion tool health. Release v0....

#### Acceptance Criteria & Specification
WHEN the user launches `ce-ai tui` and navigates to the `Workflow` tab THEN it MUST render the current 7-Stage FSM state, active task string, and checkpoint history from `state.json`. WHEN the user runs `ce-ai doctor`

---

### v0_7_0_roadmap

- **Original Folder:** `v0_7_0_roadmap`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 11/11

#### Problem Statement
While `ce-ai` v0.6.0 established proactive workflow observability, live TUI gauges, and real-time sync watchers, two critical capabilities are missing for repository isolation and lifecycle maintenance:

#### Acceptance Criteria & Specification
- **WHEN** a `.ce-ai.json` file exists in the current repository root, - **THEN** `ce-ai` MUST load `.ce-ai.json` overrides and merge them on top of global `~/.config/ce-ai/state.json`. - **WHEN** a configuration key (e.g. `installed_harnesses`) is omitted from `.ce-ai.json`,

---

### v0_8_0_roadmap

- **Original Folder:** `v0_8_0_roadmap`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 12/12

#### Problem Statement
While `ce-ai` v0.7.0 delivered repository-level workspace overrides and complete multi-harness uninstallation parity, binary compilation and installation currently require local Rust toolchains (`cargo install`). To achieve seamless distribution for end users across all major ...

#### Acceptance Criteria & Specification
- **WHEN** a release tag matching `v*` is pushed to GitHub, - **THEN** GitHub Actions MUST compile release binaries for 6 target architectures: `x86_64-linux`, `aarch64-linux`, `x86_64-macos`, `aarch64-macos` (Apple Silicon), `x86_64-windows`, and `aarch64-windows`. - **WHEN** binary compilation completes for each t...

---

### v0_9_0_roadmap

- **Original Folder:** `v0_9_0_roadmap`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 10/10

#### Problem Statement
Following the successful completion of Release v0.8.0, `ce-ai` requires a dedicated hardening phase prior to the `v1.0.0` Production Stable Release. The goal of Release v0.9.0 is to perform a comprehensive ISO 27001 / ISO 27002 security threat matrix audit, establish high-perf...

#### Acceptance Criteria & Specification
- **WHEN** an archive contains directory traversal relative or absolute paths, - **THEN** `source::archive::safe_extract` MUST reject the entry prior to writing any bytes to disk. - **WHEN** `State::save` or `write_atomic` is invoked,

---

### v1_0_0_roadmap

- **Original Folder:** `v1_0_0_roadmap`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/9

#### Problem Statement
`ce-ai` has reached production maturity across 12 AI coding agent harnesses with multi-platform binary compilation, universal installers, workspace overrides, ISO 27001/27002 security threat matrix validation, and sub-50ms benchmarks. Release `v1.0.0` represents the formal **P...

#### Acceptance Criteria & Specification
- **WHEN** any `ce-ai` subcommand (`install`, `sync`, `upgrade`, `models`, `status`, `uninstall`, `doctor`, `workflow`, `tools`, `backups`, `tui`) is invoked with established flags, - **THEN** execution MUST maintain full backwards compatibility without breaking flag signatures. ---

---

### version_update_checker

- **Original Folder:** `version_update_checker`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 0/9

#### Problem Statement
Closes #14. Harnesses with `source: local` display `version: local`, making it difficult for users to know whether they are running the latest upstream release or lagging behind.

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai status` or TUI `Status & Harnesses` is loaded, - **THEN** `ce-ai` MUST report the latest available upstream GitHub release tag alongside installed harness versions. - **WHEN** a harness is installed with `source: local`,

---

### workflow-fsm-engine

- **Original Folder:** `workflow-fsm-engine`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 7/7

#### Problem Statement
Issue #156 notes that `ce-ai workflow` currently abuses `State.last_update_check` to store an unvalidated checkpoint string (`phase | task | timestamp`). It enforces no stage transitions, validates no stages, and performs no context recovery upon `workflow resume`.

#### Acceptance Criteria & Specification
WHEN a workflow checkpoint is saved via `ce-ai workflow checkpoint`, THEN `ce-ai` SHALL store a `WorkflowState` object containing `stage`, `task`, `feature_name`, and `updated_at` under the `workflow` key in `state.json`. If an older `state.json` contains a legacy `last_update_check` string, `ce-ai` SHALL parse it t...

---

### worktree_safety_protection

- **Original Folder:** `worktree_safety_protection`
- **Archived Date:** 2026-08-24
- **Tasks Progress:** 1/6

#### Problem Statement
When pair programming with AI agents, worktrees are created outside the primary repository checkout (e.g. `../ce-ai-worktrees/model-defaults-tui`). Standard cleanup routines mistake these sibling worktrees for obsolete temporary directories, causing catastrophic data loss when...

#### Acceptance Criteria & Specification
- **WHEN** an AI agent reads `AGENTS.md`, - **THEN** it MUST encounter Rule 8 in the Hard-Gate Invariant Index prohibiting automated or unconfirmed deletion of sibling worktrees. - **WHEN** `ce-ai doctor` is executed within a Git repository,

---

### canonical-skills-adoption

- **Original Folder:** `2026-08-26-canonical-skills-adoption`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 12/12

#### Problem Statement
The compound-engineering release ships its 33 skills at top-level `skills/`, which ce-ai never harvested — no harness receives managed skill files, the sync matrix reports every surface as `registered — nothing to verify` (diagnosed 2026-08-24), and real machines accumulat...

#### Acceptance Criteria & Specification
- **WHEN** install runs against a release tree containing top-level `skills/` - **THEN** the managed directory contains `skills/...` entries recorded in the install manifest - **AND** no harness-owned skills directory receives any file

---

### close-determinism-leaks

- **Original Folder:** `2026-08-26-close-determinism-leaks`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 15/15

#### Problem Statement
The determinism audit (2026-08-24) identified two leaks inside the asset/state layer that `ce-ai` claims to control:

#### Acceptance Criteria & Specification
- **WHEN** the GitHub API query fails at the transport layer - **THEN** `install`/`upgrade` exit with code 5 (`Network`) - **AND** no tarball is downloaded - **AND** the error message names the pinned alternatives (`--to <tag>`, `--source <path>`).

---

### exit-codes-and-bench-wiring

- **Original Folder:** `2026-08-26-exit-codes-and-bench-wiring`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 5/5

#### Problem Statement
Issues #163 (P1) and #84. - #163: invariant #7 promises exit codes `3` State / `4` IO / `5` Network, but `CeError` folds them into Runtime(1)/Io(1) — automation cannot pick

#### Acceptance Criteria & Specification
- **WHEN** state.json is corrupt/unreadable/unpersistable, **THEN** ce-ai exits with code 3. - **WHEN** a remote fetch fails while propagating (tarball download),

---

### gate-hardening

- **Original Folder:** `2026-08-26-gate-hardening`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 4/4

#### Problem Statement
Issue #165 (P1): verification gates must verify. 1. `make e2e` reported PASS with Docker unavailable — the container scenario silently skipped (`println!("SKIPPED")` + early return).

#### Acceptance Criteria & Specification
- **WHEN** Docker is unavailable or unreachable, **THEN** `make e2e` MUST fail loudly (non-zero) with FAILED-TO-RUN remediation guidance — never report PASS by skipping.

---

### ideation-retention-ssot-v3

- **Original Folder:** `2026-08-26-ideation-retention-ssot-v3`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 11/11

#### Problem Statement
The SSOT rule calls ideation artifacts (`docs/brainstorms/*.md`, `docs/ideation/*.md`) "disposable inputs" without defining disposal. Because OpenSpec deliberately references (not copies) those docs, and multiple archived changes carry live links to them, agents and contributo...

#### Acceptance Criteria & Specification
Origin: `docs/brainstorms/2026-08-25-ideation-retention-ssot-clarification-requirements.md` (R1–R6, AE1–AE3) WHEN an agent or contributor reads the Single Source of Truth Rule in root `AGENTS.md`,

---

### internal-hygiene-registration-spec

- **Original Folder:** `2026-08-26-internal-hygiene-registration-spec`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 5/5

#### Problem Statement
Three debts from the v1.20.x sweep: install.rs still carried the nine-arm copy-paste chain that sync already replaced; the shared table gave cursor a skills subpath it must not have (v1.19.2 regression: sync polluted

#### Acceptance Criteria & Specification
- **WHEN** a new `HarnessKind` variant is added, **THEN** compilation fails until `harness::registration::registration_spec` classifies it. - **WHEN** install or sync runs for cursor, **THEN** no skills tree is

---

### openspec-hygiene-and-error-transparency

- **Original Folder:** `2026-08-26-openspec-hygiene-and-error-transparency`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 6/6

#### Problem Statement
Two debts from the v1.20.0 audit sweep: 1. **The OpenSpec ledger lies by omission.** There is no archive convention, so completed change folders accumulate forever in `openspec/changes/`;

#### Acceptance Criteria & Specification
- **WHEN** a change folder's feature is verifiably shipped, **THEN** its `tasks.md` MUST either have every box checked or carry a STATUS header citing the ship evidence, with any residual open boxes

---

### pr-size-boundaries

- **Original Folder:** `2026-08-26-pr-size-boundaries`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 3/3

#### Problem Statement
Issue #108: adopt Gentle AI-style LOC change boundaries as documented, auditable policy. The 400-line convention existed only implicitly in SDD task notes; nothing was documented or enforced, and correction loops were

#### Acceptance Criteria & Specification
- **WHEN** a PR exceeds 400 changed lines, **THEN** it MUST carry either a chained split or an approved size exception, plus a Changed-Lines Forecast. - **WHEN** corrections respond to review findings, **THEN** they MUST fit

---

### sync-verification-matrix-ux

- **Original Folder:** `2026-08-26-sync-verification-matrix-ux`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 6/6

#### Problem Statement
`ce-ai sync` prints a Sync Verification Matrix whose wording misleads users — especially new ones. With the current `compound-engineering` GitHub release (v3.23.3), which ships plugin-only assets for OpenCode (no managed skills tree), the matrix reads:

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai sync` runs against an install whose manifest contains no `skills/` entries - **THEN** each native harness line reads `○ <harness>: registered — ce-ai manages no skill files here (MCP companions only; nothing to hash-verify)` - **AND** the cursor line reads `○ cursor: registered — config reg...

---

### transactional-ops

- **Original Folder:** `2026-08-26-transactional-ops`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 7/7

#### Problem Statement
Issue #166 (P2): `write_atomic` protects individual files, but `install` and `sync` mutate backups → managed files → harness configs → manifests → registries → `state.json` **sequentially with no journal or rollback**. A

#### Acceptance Criteria & Specification
- **WHEN** install or sync performs any tracked filesystem mutation, **THEN** the prior content (or absence) of the target path MUST be persisted in `<config_dir>/install-journal.json` **before** the mutation.

---

### tui-e2e-zen

- **Original Folder:** `2026-08-26-tui-e2e-zen`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 4/4

#### Problem Statement
El TUI pasa 8/8 tests pero `every_tui_spawned_vector_satisfies_its_cli_contract` (src/tui.rs:1359) solo pinnea 5/15 vectores; los 6 comandos huérfanos (skills/tools/usage/audit/init-prj) nunca se ejecutan en CI y `Dockerfile.e2e` no prueba TUI. Un junior puede romper paridad ...

#### Acceptance Criteria & Specification
- **WHEN** `cargo test --test tui` runs - **THEN** the contract test asserts 15 vectors parse and fails if any new CLI flag breaks TUI.

---

### tui-upgrade-contract-fix

- **Original Folder:** `2026-08-26-tui-upgrade-contract-fix`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 3/3

#### Problem Statement
The TUI "Upgrade Release" panel still spawned `ce-ai upgrade --harness all --force`, flags removed from the CLI contract in v1.18.1 (Issue #161) — so the action failed with clap exit 2 for every user.

#### Acceptance Criteria & Specification
- **WHEN** the TUI Upgrade Release action runs, **THEN** it MUST invoke `ce-ai upgrade` with arguments accepted by the current CLI contract and MUST NOT pass --harness/--force.

---

### usage-capture-report

- **Original Folder:** `2026-08-26-usage-capture-report`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 7/7

#### Problem Statement
Issue #78: ce-ai orchestra harnesses pero no hay visibilidad de tokens consumidos ni horas humanas — bloqueando estimación y facturación tokens+horas por persona. - Módulo `src/usage/`: ledger shard-per-author (`.ce-ai/usage/<dev>.jsonl`), marcador por-proyecto local-only...

#### Acceptance Criteria & Specification
- **WHEN** pre-commit hook se ejecuta con hooks instalados, **THEN** el usage acumulado en fuentes Tier-A locales desde el último marcador se registra en `.ce-ai/usage/<dev>.jsonl` con author/timestamp/session/model/tokens. - **WHEN** no hay TTY interactivo, **THEN** solo tokens se registran (sin prompt de horas). ...

---

### workload-guard-citation

- **Original Folder:** `2026-08-26-workload-guard-citation`
- **Archived Date:** 2026-08-26
- **Tasks Progress:** 1/1

#### Problem Statement
Precision follow-up to `pr-size-boundaries` after verifying upstream: the 400-line boundary is enforced by the Review Workload Guard inside `internal/assets/hermes/sdd-orchestrator.md` (trigger conditions:

#### Acceptance Criteria & Specification
- **WHEN** citing the 400-line boundary origin, **THEN** the docs MUST name the SDD orchestrator asset hosting the Review Workload Guard triggers. - **WHEN** describing the boundary semantics, **THEN** docs MUST state it is

---

### codegraph-init-and-cleanup

- **Original Folder:** `codegraph-init-and-cleanup`
- **Archived Date:** 2026-09-07
- **Tasks Progress:** 0/5

#### Problem Statement
In `ce-ai`, users and companion tools rely on CodeGraph (`.codegraph/` index) for structural code intelligence and symbol navigation. However: 1. **Misdirected Guidance**: `ce-ai audit` hardcodes `detail: ".codegraph/ index not initialized (run 'gentle-ai codegraph init')"` ([...

#### Acceptance Criteria & Specification
- **WHEN** user runs `ce-ai tools init codegraph` in a project where `codegraph` is in PATH and `.codegraph/` does not exist: - **THEN** `ce-ai` runs `codegraph init` targeting the project directory and outputs confirmation. - **WHEN** user runs `ce-ai tools init codegraph` and `.codegraph/` already exists:

---

### codegraph-init-happy-path-tests

- **Original Folder:** `codegraph-init-happy-path-tests`
- **Archived Date:** 2026-09-07
- **Tasks Progress:** 0/3

#### Problem Statement
In `ce-ai v1.39.0`, native CodeGraph index initialization support was introduced via `ce-ai tools init codegraph` and auto-initialization in `ce-ai init-prj`. While error handling (missing binary, unsupported tools), idempotency (already-initialized index), and `--dry-run` mod...

#### Acceptance Criteria & Specification
- **WHEN** user executes `ce-ai tools init codegraph` on a directory where `.codegraph/` does not exist and `codegraph` executable is found in `PATH`: - **THEN** `ce-ai` executes `codegraph init <dir>`, exits `0`, confirms initialization on stdout, and `.codegraph/` exists on disk. - **WHEN** user executes `ce-ai in...

---

### codex-session-start-drift-delivery

- **Original Folder:** `codex-session-start-drift-delivery`
- **Archived Date:** 2026-09-07
- **Tasks Progress:** 0/19

#### Problem Statement
In `ce-ai v1.31.0`–`v1.33.0`, native Turn-0 synchronization was implemented for Claude Code, OpenCode, and GitHub Copilot CLI. For OpenAI Codex CLI, synchronization currently depends on the textual directive in `AGENTS.md` and `.codex/AGENTS.md`.

#### Acceptance Criteria & Specification
- **WHEN** `ensure_session_start_hook` is called with a path to a Codex TOML configuration file (e.g. `.codex/config.toml`), - **THEN** it MUST ensure the file contains a `[[hooks.SessionStart]]` table with `matcher = "startup|resume|compact"` and a command hook running `ce-ai workflow resume`, serialized cleanly to...

---

### cursor-session-start-drift-delivery

- **Original Folder:** `cursor-session-start-drift-delivery`
- **Archived Date:** 2026-09-07
- **Tasks Progress:** 0/7

#### Problem Statement
When developing in Cursor (both desktop IDE and Cursor CLI v0.45+), agent sessions may experience Turn-0 drift if the LLM overlooks or forgets instructions in `AGENTS.md` or `.cursor/rules/compound-engineering.mdc`. Without an automated, synchronous lifecycle hook, `ce-ai work...

#### Acceptance Criteria & Specification
- WHEN `hooks_path` does not exist OR is invalid JSON, THEN `has_session_start_hook` returns `false`. - WHEN `hooks_path` contains `hooks.sessionStart` array with an entry where `command == "ce-ai workflow resume --json"`, THEN `has_session_start_hook` returns `true`. - WHEN `has_session_start_hook` is true, THEN `e...

---

### pedagogical-guardrail-mode

- **Original Folder:** `pedagogical-guardrail-mode`
- **Archived Date:** 2026-09-07
- **Tasks Progress:** 0/11

#### Problem Statement
Junior developers using AI coding agents increasingly fall into **vibe coding**: integrating generated code without understanding its mechanics, architecture, or edge cases. This introduces invisible technical debt, weakens engineering competence, and undermines effective huma...

#### Acceptance Criteria & Specification
- **REQ-GUARD-1.1 (Default Activation):** - **WHEN** the user executes `ce-ai guard enable` without arguments, - **THEN** `state.json` MUST be updated atomically with `guardrail: { enabled: true, level: "junior", harness: null, updated_at: "<ISO8601>" }` and return exit code 0.

---

### pi-session-start-drift-delivery

- **Original Folder:** `pi-session-start-drift-delivery`
- **Archived Date:** 2026-09-07
- **Tasks Progress:** 0/19

#### Problem Statement
`ce-ai` provides 0-step Turn-0 `RepoState` synchronization for Claude Code, OpenCode, GitHub Copilot CLI, and OpenAI Codex CLI via native lifecycle mechanisms. For Mario Zechner's Pi coding agent (`pi.dev`), synchronization currently relies solely on prompt-based directives in...

#### Acceptance Criteria & Specification
- **WHEN** `ensure_session_start_hook` is called with the target path to `.pi/extensions/compound-engineering.ts`, - **THEN** it MUST ensure the file exists containing the canonical TypeScript extension subscribing to `session_start` and `before_agent_start`, executed atomically via `write_atomic`. - **WHEN** the ex...

---

### session-start-drift-delivery

- **Original Folder:** `session-start-drift-delivery`
- **Archived Date:** 2026-09-07
- **Tasks Progress:** 0/15

#### Problem Statement
In `ce-ai v1.30.0`, `workflow resume` implemented live sub-15ms `RepoState` drift detection (Git working tree, branch, manifest SHA256 integrity, and OpenSpec progress). However, invocation was purely manual: 1. No native harness lifecycle hooks (`SessionStart`, etc.) were ins...

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai init-prj` is executed on a project containing a `.claude/` directory, - **THEN** it MUST ensure `.claude/settings.json` contains a `SessionStart` hook matching `.*` with command `ce-ai workflow resume`. - **WHEN** `.claude/settings.json` already contains user hooks or configuration keys,

---

### antigravity-pre-invocation-drift-delivery

- **Original Folder:** `antigravity-pre-invocation-drift-delivery`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 7/7

#### Problem Statement
Google Antigravity CLI does not possess a literal "session start" lifecycle hook event. However, it provides a native `PreInvocation` hook that fires before the model is invoked and explicitly supports context injection via an `injectSteps` array containing `ephemeralMessage` ...

#### Acceptance Criteria & Specification
- WHEN `hooks_path` does not exist OR contains invalid JSON, THEN `has_pre_invocation_hook` returns `false`. - WHEN `hooks_path` contains an entry with `command == "ce-ai workflow resume --pre-invocation"`, THEN `has_pre_invocation_hook` returns `true`. - WHEN `has_pre_invocation_hook` is true, THEN `ensure_pre_invo...

---

### auto-configure-rtk-hook-injection

- **Original Folder:** `auto-configure-rtk-hook-injection`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 26/26

#### Problem Statement
`rtk` (CLI output token reduction engine) currently has **no auto-registration mechanism** in `ce-ai`. Unlike companion MCP servers (`codegraph`, `engram`) which are wired into harness JSON/TOML configuration files during `ce-ai install` and `ce-ai sync`, `rtk` is a stdout pre...

#### Acceptance Criteria & Specification
- **WHEN** `is_rtk_supported(kind)` is evaluated, - **THEN** it MUST return `true` exclusively for `HarnessKind::Claude`, `HarnessKind::Cursor`, `HarnessKind::Copilot`, and `HarnessKind::Codex`. - **THEN** it MUST return `false` for all other `HarnessKind` variants (`Opencode`, `Pi`, `Custom`, `Deepseek`, `Grok`, `K...

---

### ce-code-review-mid-tier-slot

- **Original Folder:** `ce-code-review-mid-tier-slot`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 18/18

#### Problem Statement
In `compound-engineering-v3.24.0`, the `ce-code-review` skill introduced persona-level model tiering: three high-stakes reviewers (`correctness-reviewer`, `security-reviewer`, `adversarial-reviewer`) inherit the session model, while every other persona (~15+ sub-agent reviewer...

#### Acceptance Criteria & Specification
- **WHEN** a user executes `ce-ai models set --harness opencode ce-code-review-mid-tier <provider/model>`, - **THEN** `ce-ai` MUST persist the assignment to `state.json` (`model_assignments.ce-code-review-mid-tier`). - **THEN** `ce-ai` MUST persist the assignment to `opencode.json` under `agent.ce-code-review-mid-ti...

---

### close-engram-codegraph-registration-parity-gap

- **Original Folder:** `close-engram-codegraph-registration-parity-gap`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 23/23

#### Problem Statement
Companion MCP servers (`codegraph` and `engram`) are auto-registered during `ce-ai install` and `ce-ai sync` for native harnesses managed via the strategy table `registration_spec` in `src/harness/registration.rs` (Claude, Codex, Copilot, Grok, Kimi, Agy, Fx, Cursor). However,...

#### Acceptance Criteria & Specification
WHEN `ce-ai install` or `ce-ai sync` runs for `HarnessKind::Opencode` THEN `opencode.json` SHALL contain MCP server entries for `codegraph` (`command: "codegraph", args: ["mcp"]`) and `engram` (`command: "engram", args: ["serve"]`) under `mcpServers`. WHEN `ce-ai install` or `ce-ai sync` runs for `HarnessKind::Custom`

---

### copilot-session-start-drift-delivery

- **Original Folder:** `copilot-session-start-drift-delivery`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 22/22

#### Problem Statement
In `ce-ai v1.31.0` and `v1.32.0`, native Turn-0 synchronization was delivered for Claude Code and OpenCode. For GitHub Copilot CLI, agents still rely on textual prompt directives in `AGENTS.md` and `.github/copilot-instructions.md`.

#### Acceptance Criteria & Specification
- **WHEN** `ensure_session_start_hook` is called with a path to a Copilot hooks configuration file (e.g. `.github/hooks/hooks.json`), - **THEN** it MUST ensure the file exists, has `"version": 1`, and contains a `sessionStart` hook command executing `ce-ai workflow resume --json` under both `bash` and `powershell` k...

---

### doctor-git-hooks-convention

- **Original Folder:** `doctor-git-hooks-convention`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 13/13

#### Problem Statement
The `ce-ai doctor` command includes a git-hooks verification probe (`src/commands/doctor.rs:297-333`) originally created as a self-check for `ce-ai`'s own repository convention (guarding against git worktree contention resetting `core.hooksPath`). However, because it runs unco...

#### Acceptance Criteria & Specification
- **WHEN** a repository has `core.hooksPath` set to a path other than `.githooks` (such as `.husky/_`) AND no `.githooks/` directory exists at the repository root - **THEN** `ce-ai doctor` SHALL NOT record a finding for `git-hooks: core.hooksPath` - **AND** `ce-ai doctor` SHALL output an informational message: `doct...

---

### doctor-mcp-tools-detection

- **Original Folder:** `doctor-mcp-tools-detection`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 16/16

#### Problem Statement
`ce-ai doctor` and `ce-ai tools status` report companion tools like `context7` as `not found` (or `tool-missing` in `--strict` mode) and unconditionally print skill suggestions for `sequential-thinking`, even when both are already configured as MCP servers in the active harnes...

#### Acceptance Criteria & Specification
- **WHEN** a companion tool (such as `context7`) is registered under `mcpServers` in an active harness configuration file (e.g. `opencode.json`) - **AND** no binary for the tool exists on `PATH` - **THEN** `ce-ai doctor` SHALL report the companion tool as OK (`doctor-info: <name> v<version> (ok)`)

---

### doctor-native-marketplace-divergence

- **Original Folder:** `doctor-native-marketplace-divergence`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 20/20

#### Problem Statement
`ce-ai` manages its own isolated content tree for `compound-engineering` across supported harnesses (`~/.claude/compound-engineering/`, `~/.config/opencode/compound-engineering/`, etc.) tracked via `state.json` (`installed_harnesses` and `release_provenance`).

#### Acceptance Criteria & Specification
- **WHEN** `<claude_dir>/plugins/installed_plugins.json` does not exist or cannot be read - **THEN** `check_claude_marketplace_divergence` returns an empty vector, and `ce-ai doctor` proceeds without error. - **WHEN** the `claude` harness is not registered in `state.installed_harnesses` (or its workspace scope does ...

---

### doctor-workspace-scope-manifest

- **Original Folder:** `doctor-workspace-scope-manifest`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 13/13

#### Problem Statement
When `ce-ai install --harness opencode --scope workspace` is executed (as recommended for isolated Git worktrees in `docs/user-guide/quick-start-workflow-guide.md`), `install-manifest.json` and OpenCode plugin assets are written to the workspace root (`<repo_root>/compound-eng...

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai install --harness opencode --scope workspace` is executed, **THEN** the entry added to `state.installed_harnesses` MUST include `"scope": "workspace"` and `"target_dir": "<workspace_root>"`. - **WHEN** `ce-ai install --harness opencode` (global scope default) is executed,

---

### engram-context-echo-behavioral-risk

- **Original Folder:** `engram-context-echo-behavioral-risk`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 14/14

#### Problem Statement
During the investigation of Issue #313, a recurring symptom was analyzed where the `ce-ai` workflow FSM progress banner (`tasks progress: 0/N completed`) appeared to stubbornly persist across distinct agent sessions even after work had progressed.

#### Acceptance Criteria & Specification
- **WHEN** `docs/solutions/architecture/engram-context-echo-behavioral-risk.md` is authored, - **THEN** it MUST contain complete YAML front-matter with `category: architecture`, `tags`, `components`, and `applies_when`. - **AND** it MUST cite `src/commands/workflow.rs` (lines 48, 235, 725, 862) demonstrating zero de...

---

### extract-inline-unit-tests

- **Original Folder:** `extract-inline-unit-tests`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 51/51

#### Problem Statement
Currently, unit tests in `ce-ai` are embedded inline inside the same source files as production code using `#[cfg(test)] mod tests { ... }` blocks across 36 files in `src/`. This represents approximately **4,367 lines (~32%)** of test code interleaved directly into functional ...

#### Acceptance Criteria & Specification
- **WHEN** any source file `src/**/*.rs` is compiled in test mode (`cargo test`), - **THEN** all unit tests MUST execute from their respective extracted test files located under `src/**/tests/*.rs` or `src/tests/*.rs`. - **WHEN** running `cargo test` and `make e2e` across all platforms (Linux, macOS, Windows),

---

### fix-init-prj-dry-run-output

- **Original Folder:** `fix-init-prj-dry-run-output`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 6/6

#### Problem Statement
`ce-ai --dry-run init-prj` is correctly non-destructive, but it reports the **same success line as a real run** (`✓ Adopted project ...`) and omits the RTK hook actions a real run performs. Users cannot trust dry-run to preview impact. Found while dogfooding `init-prj` on th...

#### Acceptance Criteria & Specification
- WHEN `ce-ai --dry-run init-prj <path>` runs against a project without an up-to-date managed block: - THEN stdout MUST contain a line matching `dry-run: would adopt project at`. - AND stdout MUST NOT contain `✓ Adopted project`.

---

### grok-turn-0-drift-delivery-evaluation

- **Original Folder:** `grok-turn-0-drift-delivery-evaluation`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 3/3

#### Problem Statement
Evaluate whether Grok Build CLI (`grok`) supports native Turn-0 conversational context injection via `SessionStart` hooks (`~/.grok/hooks/*.json` or `.grok/hooks/*.json`) to eliminate Turn-0 drift automatically.

#### Acceptance Criteria & Specification
- WHEN Grok CLI hook capabilities are evaluated, - THEN the finding MUST be documented in `docs/solutions/architecture/2026-09-02-grok-harness-turn-0-drift-delivery-evaluation.md`. - WHEN `ce-ai init-prj` adopts a project for Grok,

---

### hardening-inferred-stage-monotonicity

- **Original Folder:** `hardening-inferred-stage-monotonicity`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 9/9

#### Problem Statement
In Issue #296 (PR #305, v1.40.0), Workflow FSM auto-checkpointing was introduced across 7 harnesses. While researching the edge cases, an architectural defense-in-depth gap was identified (Issue #306):

#### Acceptance Criteria & Specification
WHEN `validate_and_set_workflow_for_branch` is called with `source = WorkflowSource::Inferred` AND the workspace/branch already has an existing workflow at `current_stage` AND `target_stage.number() < current_stage.number()`

---

### hermetic-git-test-fixtures

- **Original Folder:** `hermetic-git-test-fixtures`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 3/3

#### Problem Statement
In Issue #302, 3 tests in `tests/cli.rs` were observed failing intermittently during local pre-commit hook executions (`.githooks/pre-commit`): - `audit_suggests_codegraph_init_without_gentle_ai` - `doctor_workspace_scope_opencode_install_has_no_false_positive_findings` - `ins...

#### Acceptance Criteria & Specification
- **WHEN** test functions in `tests/cli.rs` execute git commands to construct or inspect repositories, - **THEN** the execution SHALL use `git_cmd()` with `GIT_DIR`, `GIT_WORK_TREE`, `GIT_INDEX_FILE`, and `GIT_PREFIX` removed from the command's environment. - **WHEN** `cargo test --test cli` runs within an environme...

---

### install-directory-config-mcp-guard

- **Original Folder:** `install-directory-config-mcp-guard`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 5/5

#### Problem Statement
When running `ce-ai install --harness all` (or `ce-ai sync`), `ce-ai` iterates through all detected harnesses and configures companion MCP servers (`codegraph`, `engram`) via `RegistrationSpec::register_companions`.

#### Acceptance Criteria & Specification
- **WHEN** `RegistrationSpec::register_companions` is invoked with a `target_config` path that exists and is a directory (`target_config.exists() && !target_config.is_file()`), - **THEN** it MUST NOT call the underlying vendor MCP registrar. - **AND** it MUST emit a diagnostic warning to stderr in the format:

---

### journal-append-only-linear-arm

- **Original Folder:** `journal-append-only-linear-arm`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 27/27

#### Problem Statement
In `src/state/journal.rs`, `Journal::arm` re-serializes the entire operation journal history on every filesystem mutation: ```rust pub fn arm(&mut self, path: &Path) -> Result<(), CeError> { self.writes_seen += 1; let prior = std::fs::read(path).ok(); self.data.ops.push(Record...

#### Acceptance Criteria & Specification
- **WHEN** `Journal::begin` initializes a fresh journal at `install-journal.json`, - **THEN** it MUST write an initial single-line JSON header containing `command` and `started_at` terminated by a newline (`\n`). - **AND** each subsequent mutation recorded via `Journal::arm` MUST be appended as an individual single-...

---

### kimi-turn-0-drift-delivery-evaluation

- **Original Folder:** `kimi-turn-0-drift-delivery-evaluation`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 3/3

#### Problem Statement
Evaluate whether Kimi Code CLI (`kimi`) supports native project-level Turn-0 conversational context injection via `SessionStart` hooks (`[[hooks]]` in `config.toml`) to automatically eliminate Turn-0 drift on project adoption.

#### Acceptance Criteria & Specification
- WHEN Kimi Code CLI hook capabilities are evaluated, - THEN the finding MUST be documented in `docs/solutions/architecture/2026-09-02-kimi-harness-turn-0-drift-delivery-evaluation.md`. - WHEN `ce-ai init-prj` adopts a project for Kimi,

---

### observe-only-gate-check

- **Original Folder:** `observe-only-gate-check`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 14/14

#### Problem Statement
Issue #332 originally proposed a blocking gate (hook + validation receipt) to prevent code writes when OpenSpec contracts are absent. However, design review revealed a critical foundational flaw: **there is zero measured empirical evidence that the gap exists**. The only evide...

#### Acceptance Criteria & Specification
- **WHEN** the environment variable `CE_AI_DISABLE_GATE_CHECK=1` or `CE_AI_GATE_CHECK_DISABLED=1` is present, OR the `--disabled` flag is provided to `ce-ai gate check`, - **THEN** `ce-ai gate check` MUST exit immediately with code `0`, performing no state loading, no filesystem traversal, and no log writes. - **WHE...

---

### opencode-session-start-drift-delivery

- **Original Folder:** `opencode-session-start-drift-delivery`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 26/26

#### Problem Statement
In `ce-ai v1.31.0`, Turn-0 `RepoState` synchronization was guaranteed for Claude Code via native `.claude/settings.json` `SessionStart` hooks. In OpenCode, synchronization remains an un-enforced textual directive in `AGENTS.md`. If the agent starts a session without invoking `...

#### Acceptance Criteria & Specification
- **WHEN** an OpenCode session is initialized (`session.created`), - **THEN** the plugin MUST execute `ce-ai workflow resume` in the session's workspace directory and inject the resulting output into the session via `client.session.prompt` with `{ noReply: true }`. - **WHEN** an OpenCode session undergoes context co...

---

### openspec-archive-command

- **Original Folder:** `openspec-archive-command`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 15/15

#### Problem Statement
In `ce-ai v1.44.2` (Issue #323), `probe_unarchived_completed_changes` was introduced to detect change packages under `openspec/changes/` where all tasks in `tasks.md` are marked complete (`- [x]`). However, automatic directory moving was deliberately excluded from that probe t...

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai workflow archive <feature>` (or `ce-ai archive <feature>`) is executed on a feature directory where all tasks in `tasks.md` are marked `- [x]` or `- [X]`, - **THEN** it MUST move the directory from `openspec/changes/<feature>` to `openspec/changes/archive/<feature>`. - **THEN** it MUST emit a succe...

---

### self-update

- **Original Folder:** `self-update`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 15/15

#### Problem Statement
`ce-ai upgrade` (and the TUI Upgrade Release flow) exclusively upgrades the compound-engineering plugin across registered harnesses (`~/.config/opencode/compound-engineering/`, etc.). There is currently no in-tool command to update the `ce-ai` CLI binary itself.

#### Acceptance Criteria & Specification
- **R1.1**: The system MUST detect the host architecture and operating system and map it to one of the 6 supported release targets: - `x86_64-unknown-linux-gnu` - `aarch64-unknown-linux-gnu`

---

### sequential-thinking-skill

- **Original Folder:** `sequential-thinking-skill`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 7/7

#### Problem Statement
In `ce-ai`, `sequential-thinking` is currently defined as an on-demand companion skill suggestion in [`src/source/tools_registry.rs`](file:///Users/mastepanoski/projects/web/ai/ce-ai/src/source/tools_registry.rs#L116-L124) (`name: "sequential-thinking"`, `resolve_cmd: "ce-ai s...

#### Acceptance Criteria & Specification
- WHEN `skills/sequential-thinking/SKILL.md` is inspected, THEN it MUST exist in the repository tree and contain non-empty markdown documentation defining a structured reasoning protocol. - WHEN `skills/sequential-thinking/SKILL.md` is parsed by `parse_skill_frontmatter`, THEN: - `fm.name` MUST equal `"sequential-th...

---

### ship-readiness-observe-only

- **Original Folder:** `ship-readiness-observe-only`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 8/8

#### Problem Statement
Issue #354: the 7-stage cycle is not enforced at the Ship boundary. An agent moved Verify -> Ship for #351/#352, skipping Stage 6 (`ce-compound`) and `ce-code-review`. The skip was invisible until a human asked, and the unreviewed first attempt contained a real P1. Today `ce-a...

#### Acceptance Criteria & Specification
- WHEN an **adopted** workspace has `commits_ahead > 0` relative to the resolved base, THEN ship readiness MUST evaluate and report: - commits ahead count, - Stage 6 artifact presence (`docs/solutions/**.md` added/modified on the branch, or a dirty file that still exists on disk),

---

### skills-resolve-positional-query

- **Original Folder:** `skills-resolve-positional-query`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 3/3

#### Problem Statement
When running `ce-ai doctor` or `ce-ai tools status`, `ce-ai` recommends installation/resolution commands for companion tools such as `sequential-thinking`. The suggested command is: ```bash ce-ai skills resolve sequential-thinking ``` However, running this exact command fails ...

#### Acceptance Criteria & Specification
- **WHEN** the user invokes `ce-ai skills resolve <QUERY>` (e.g. `ce-ai skills resolve sequential-thinking`), - **THEN** `ce-ai` SHALL accept the argument without a Clap parsing error, resolve `<QUERY>` against the skills and companion tools registries, and output the resolved prompt / configuration instructions. - ...

---

### sync-non-opencode-harnesses

- **Original Folder:** `sync-non-opencode-harnesses`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 14/14

#### Problem Statement
Currently, `ce-ai sync` and `ce-ai upgrade` treat OpenCode's install manifest (`<opencode_dir>/compound-engineering/install-manifest.json`) as an unconditional precondition for execution: ```rust let manifest = InstallManifest::load(&opencode_dir) .map_err(|_| CeError::Runtime...

#### Acceptance Criteria & Specification
WHEN `ce-ai install --harness <kind>` is run for a non-OpenCode harness (e.g. `claude`, `copilot`, `cursor`, `pi`) AND `ce-ai sync` is executed without OpenCode installed THEN `ce-ai sync` SHALL exit with code 0

---

### sync-verification-and-token

- **Original Folder:** `sync-verification-and-token`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 17/17

#### Problem Statement
When running `ce-ai upgrade` to update to the latest version of the Compound Engineering plugin (e.g. `compound-engineering-v3.24.0`), users encounter two consecutive blocking failure modes:

#### Acceptance Criteria & Specification
- **WHEN** `github_token_from_env()` is invoked, - **THEN** it MUST inspect environment variables in priority order: 1. `CE_AI_GITHUB_TOKEN`

---

### unarchived-completed-changes-detection

- **Original Folder:** `unarchived-completed-changes-detection`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 15/15

#### Problem Statement
In `ce-ai v1.44.0` (Issue #313), a tasks-desync probe was introduced to reconcile modified code with OpenSpec task checkboxes. However, that probe was strictly scoped to a single active feature resolved by `probe_openspec_context_in`. It never scanned sibling directories under...

#### Acceptance Criteria & Specification
- **WHEN** `count_task_checkboxes` is called with a path to a `tasks.md` file, - **THEN** it MUST parse each line, counting lines starting with `- [x]` or `- [X]` as completed tasks and lines starting with `- [ ]` as incomplete tasks, returning `(completed_tasks, total_tasks)`. - **WHEN** the given path does not exi...

---

### workflow-fsm-auto-checkpoint

- **Original Folder:** `workflow-fsm-auto-checkpoint`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 32/32

#### Problem Statement
The 7-stage Workflow FSM (`ce-ai workflow status` / `checkpoint` / `resume`, implemented in `src/commands/workflow.rs` and `src/state/state.rs`) is designed as a resilience and recovery mechanism for context-window exhaustion and session hand-offs. However, in real-world agent...

#### Acceptance Criteria & Specification
- **WHEN** `docs/ideation/` or `docs/brainstorms/*.md` exists and no `openspec/changes/<feature>/` directory exists, - **THEN** `infer_workflow_stage` SHALL deduce `WorkflowStage::Ideation`. - **WHEN** `openspec/changes/<feature>/` contains `proposal.md` and `spec.md`, but either `tasks.md` is absent or `total_tasks...

---

### workflow-reset-feature-clearing

- **Original Folder:** `workflow-reset-feature-clearing`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 6/6

#### Problem Statement
When resetting the 7-stage workflow FSM to Stage 1 (Ideation) from an advanced stage (e.g. Stage 2+), `validate_and_set_workflow` unconditionally inherits the previous workflow's `feature_name` if `--feature` is omitted. Consequently, subsequent `workflow resume` invocations r...

#### Acceptance Criteria & Specification
- WHEN `validate_and_set_workflow` is called with `target_stage == WorkflowStage::Ideation` AND `current_stage != WorkflowStage::Ideation` AND `feature` is `None`, THEN `WorkflowState.feature_name` is set to `None`. - WHEN `validate_and_set_workflow` is called with `target_stage != WorkflowStage::Ideation` OR `curre...

---

### workflow-tasks-desync

- **Original Folder:** `workflow-tasks-desync`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 7/7

#### Problem Statement
In `ce-ai`, the workflow Finite State Machine (`infer_stage_from_repo` in [`src/commands/workflow.rs`](file:///Users/mastepanoski/projects/web/ai/ce-ai/src/commands/workflow.rs#L725-L807)) derives progress across Stages 3–7 by inspecting checkbox completion in `openspec/chan...

#### Acceptance Criteria & Specification
- WHEN `reconcile_tasks_with_git` or `extract_paths_from_task_text` parses a markdown task item (e.g. `- [ ] 1. Implement foo in `src/commands/workflow.rs``), THEN: - It MUST extract all backticked code spans. - It MUST extract path-like tokens containing directory separators (`/`) or known file extensions (`.rs`, `...

---

### workspace-scoped-workflow-and-gitignore

- **Original Folder:** `workspace-scoped-workflow-and-gitignore`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 19/19

#### Problem Statement
1. **Global Workflow Collision in FSM**: `State.workflow: Option<WorkflowState>` in `src/state/state.rs` stores a single global workflow checkpoint in `~/.ce-ai/state.json`. When developers work across multiple repositories or multiple git worktrees on the same machine (a work...

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai workflow checkpoint` is executed in a repository or worktree `A`, - **THEN** `ce-ai` MUST save the `WorkflowState` under the workspace key corresponding to `A` in `state.workflows` within `~/.ce-ai/state.json`. - **WHEN** `ce-ai workflow checkpoint` is subsequently executed in another repository or...

---

### zero-friction-release-fallback

- **Original Folder:** `zero-friction-release-fallback`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 17/17

#### Problem Statement
When running `ce-ai install` or `ce-ai upgrade` without an explicit GitHub Personal Access Token or authenticated GitHub CLI (`gh`), `ce-ai` queries the GitHub REST API (`https://api.github.com/repos/everyinc/compound-engineering-plugin/releases`). GitHub applies an aggressive...

#### Acceptance Criteria & Specification
- **WHEN** `resolve_latest_release` is invoked without a token or when GitHub REST API returns `403 Forbidden` / `429 Too Many Requests` - **THEN** it MUST query `https://github.com/everyinc/compound-engineering-plugin/releases/latest` and extract the target tag from the resolved redirect URL. - **WHEN** Web Redirec...

---

### zero-step-drift-recovery

- **Original Folder:** `zero-step-drift-recovery`
- **Archived Date:** 2026-09-11
- **Tasks Progress:** 21/21

#### Problem Statement
Standard AI agent runtimes condition future tool execution on accumulated conversational history. When external actors modify the workspace outside the agent's turn—such as human git commits, branch checkouts, IDE file renames, or manual dependency upgrades—the agent's int...

#### Acceptance Criteria & Specification
- **WHEN** a user or agent invokes `ce-ai workflow resume` in a clean repository on branch `main` with valid manifest and no uncommitted files: - **THEN** `ce-ai` MUST output `== [Environment State & Drift Status] ==` reporting: - `git branch: main` - `working tree: clean (0 uncommitted changes)` - `manifest integri...

---

### blocking-gate-check

- **Original Folder:** `blocking-gate-check`
- **Archived Date:** 2026-09-12
- **Tasks Progress:** 24/24

#### Problem Statement
In Spec-Driven Development and Compound Engineering, production code under `src/**` should only be written during Stage 4 (`ce-work`) when formal specifications (`proposal.md`, `spec.md`, `tasks.md`) have been approved. In Issue #333 (v1.49.0), an observe-only spike (`ce-ai ga...

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai gate check` evaluates a tool write in Stage 4 (`ce-work`) under `src/**` for an adopted project, AND the active feature is missing `proposal.md`, `spec.md`, or `tasks.md`, AND `gate_mode` is `enforce`, **THEN** `ce-ai gate check` MUST exit with process exit code `2`, print the blocking remediation ...

---

### archive-compaction

- **Original Folder:** `archive-compaction`
- **Archived Date:** 2026-09-13
- **Tasks Progress:** 33/33

#### Problem Statement
In Compound Engineering, `openspec/changes/archive/` serves as the project's permanent audit record linking shipped releases to their frozen contracts. Over prolonged development cycles, loose package directories accumulate rapidly: 1. **Archive Directory Sprawl:** `ce-ai` cur...

#### Acceptance Criteria & Specification
- **WHEN** `ce-ai archive compact --dry-run` is invoked: - **THEN** it resolves all loose packages in `openspec/changes/archive/`. - **AND** it groups eligible packages by milestone (default quarterly or `--milestone`). - **AND** it displays the planned rollup summary paths and tarball paths to stdout. - **AND** it ...

---

### doc-debt-engine-and-probes

- **Original Folder:** `doc-debt-engine-and-probes`
- **Archived Date:** 2026-09-13
- **Tasks Progress:** 31/31

#### Problem Statement
In Compound Engineering, documentation acts as the project's living contract and persistent memory layer. As code evolves rapidly across releases, documentation inevitably accumulates technical debt: 1. **Subtask Desync Stranding Completed Features:** `probe_unarchived_complet...

#### Acceptance Criteria & Specification
- **WHEN** any documentation probe runs in a repository with `.git` and encounters no debt: - **THEN** it returns `ProbeStatus::Clean`. - **WHEN** a probe evaluates a check requiring Git history in a non-git directory: - **THEN** it returns `ProbeStatus::Unknown`. - **AND** under no circumstance does it return `Prob...

---

