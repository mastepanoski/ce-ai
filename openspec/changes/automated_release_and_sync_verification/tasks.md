# OpenSpec Tasks: Automated Release & Sync Verification

- [ ] **Unit 1: GitHub Release Automation Workflow (`.github/workflows/release.yml`)**
  - [ ] Add `.github/workflows/release.yml` with automated patch tagging and binary release publisher.
- [ ] **Unit 2: Sync Verification Matrix & Audit Report (`src/commands/sync.rs`)**
  - [ ] Update `sync_with` to compute and print itemized verification matrix per harness.
- [ ] **Unit 3: Integration Tests (`tests/cli.rs`)**
  - [ ] Test sync verification output in `tests/cli.rs`.
