# Spec Delta
- **WHEN** citing the 400-line boundary origin, **THEN** the docs MUST name
  the SDD orchestrator asset hosting the Review Workload Guard triggers.
- **WHEN** describing the boundary semantics, **THEN** docs MUST state it is
  a review-workload split signal, not an authoring cap.
